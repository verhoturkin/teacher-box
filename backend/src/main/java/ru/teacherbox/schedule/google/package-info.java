/**
 * The teacher's Google Calendar: OAuth connection and one-way sync of lessons into a calendar the
 * portal creates.
 */
@NullMarked
package ru.teacherbox.schedule.google;

import org.jspecify.annotations.NullMarked;
