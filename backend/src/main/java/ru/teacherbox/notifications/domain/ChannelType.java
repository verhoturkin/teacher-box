package ru.teacherbox.notifications.domain;

/** External messengers that can receive notifications (the personal area inbox is always on). */
public enum ChannelType {
    TELEGRAM,
    VK,
    MAX
}
