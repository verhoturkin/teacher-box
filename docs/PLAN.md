# План реализации Teacher Box

Пошаговый план. Каждый подэтап завершается зелёным `./scripts/verify.sh` и коммитом
(см. AGENTS.md §8–9). Отмечайте `[x]` в том же коммите, что и код.

Легенда: **B** — backend, **F** — frontend, **D** — docker/инфраструктура.

---

## Этап 0. Основа проекта и документация

- [x] 0.1 `AGENTS.md`, `CLAUDE.md` — правила, архитектура, соглашения.
- [x] 0.2 `docs/PLAN.md` — этот план.
- [x] 0.3 ADR: 0001 модульный монолит, 0002 встраиваемая БД, 0003 аутентификация,
      0004 варианты поставки, 0005 уведомления, 0006 ИИ, 0007 версии и лицензия frontend.
- [x] 0.4 `.gitignore`, `.gitattributes`, `.editorconfig`, README (кратко).

## Этап 1. Каркас backend

- [x] 1.1 **B** Maven-проект `backend/`: Spring Boot 4.1, Java 25, Maven Wrapper,
      зависимости (webmvc, security, oauth2-resource-server, jdbc, flyway, h2,
      validation, actuator, modulith), JaCoCo с порогами 90/80.
- [x] 1.2 **B** Пакеты-модули `shared`, `platform`, `identity`, `billing`, `homework`,
      `notifications`, `ai` с `package-info.java` (`@ApplicationModule`, `@NullMarked`).
- [x] 1.3 **B** `shared`: `Ids` (UUID v7), `Money`, `CurrentUser`/`Role`, доменные исключения
      (`NotFoundException`, `ConflictException`, `ForbiddenException`, `BusinessRuleException`),
      `FileStorage` (интерфейс), `JwtClaims`.
- [x] 1.4 **B** H2 file mode (`<data-dir>/db`), `shared/persistence/ModuleMigrations`:
      per-module Flyway (своя схема + своя history-таблица), JDBC-бины ждут миграций.
- [x] 1.5 **B** `platform/web`: `ProblemDetail`-обработчик ошибок, `platform/storage`:
      `LocalFileStorage` с namespace'ами модулей и защитой от path traversal.
- [x] 1.6 **B** Архитектурные тесты: `ModularityTests` (verify + Documenter),
      ArchUnit (domain без Spring, нет field injection, контроллеры только в `web`,
      модули не зависят от `platform`), smoke-тест контекста.
- [x] 1.7 **B** `application.yaml` (переопределение через env `TEACHERBOX_*`), actuator `health`/`info`.
- [x] 1.8 **B** `platform/security`: stateless filter chain по URL-конвенциям, HS256-ключ
      (env или файл в `/data/keys`), `JwtEncoder`/`JwtDecoder`, `PasswordEncoder`,
      резолвер `CurrentUser`, 401/403 в формате ProblemDetail.

## Этап 2. Каркас frontend

- [x] 2.1 **F** Angular 21 LTS workspace `frontend/` (standalone, zoneless, SCSS, routing),
      строгий `tsconfig`, `strictTemplates`.
- [x] 2.2 **F** PrimeNG 21 (MIT, см. ADR-0007) + Aura, русская локаль PrimeNG и Angular (`ru`), primeicons.
- [x] 2.3 **F** ESLint (angular-eslint + typescript-eslint type-checked): запрет `any`,
      `no-unsafe-*`, границы фич (`no-restricted-imports`).
- [x] 2.4 **F** Vitest + coverage-пороги 90/80, `npm test` в режиме однократного прогона.
- [x] 2.5 **F** `core/`: `AppConfig`, `ApiErrorInterceptor` (ProblemDetail → Toast),
      layout-оболочки учителя и ученика (Menubar/Sidebar), страница 404.
- [x] 2.6 **F** `proxy.conf.json` для dev (`/api` → `localhost:8080`).

## Этап 3. Сборка и Docker

- [x] 3.1 **D** `scripts/verify.sh` (backend/frontend/all) и `.githooks/pre-commit`
      (проверяет только затронутые части).
- [x] 3.2 **D** `docker/Dockerfile` target `backend` — multi-stage (Maven → JRE 25), non-root,
      volume `/data`, healthcheck.
- [x] 3.3 **D** target `frontend` — multi-stage (Node → nginx-unprivileged),
      `docker/nginx/default.conf.template`: SPA fallback, прокси `/api` → backend, gzip, security headers.
- [x] 3.4 **D** `compose.split.yaml` — вариант 1 (два контейнера, сеть, volume, healthchecks).
- [x] 3.5 **B/D** Maven-профиль `bundle-frontend` (кладёт собранный фронт в `static/`),
      SPA-fallback в `platform/web`; target `single`;
      `compose.single.yaml` — вариант 2 (один контейнер).
- [x] 3.6 **D** `.env.example` со всеми переменными, smoke-проверка обоих вариантов
      (`/actuator/health`, открывается `/`).

## Этап 4. Подсистема аутентификации (`identity`)

- [x] 4.1 **B** Схема `identity`: `users` (id, role, login, password_hash, display_name,
      email, phone, status, failed_attempts, locked_until, version), `invites`,
      `refresh_tokens`. Инвариант «учитель ровно один».
- [x] 4.2 **B** Bootstrap учителя из `TEACHERBOX_IDENTITY_TEACHER_LOGIN/PASSWORD` при первом старте
      (если пароль не задан — сгенерировать и один раз вывести в лог).
- [x] 4.3 **B** Выпуск JWT (инфраструктура ключа — в 1.8); access-токен 15 мин (в памяти SPA); refresh-токен — случайный,
      хранится хешем, HttpOnly+SameSite=Strict cookie, ротация, детект повторного использования.
- [x] 4.4 **B** `POST /api/auth/login|refresh|logout`, защита от перебора (блокировка
      после N неудач), `POST /api/me/password` (завершает все сессии, выдаёт новую текущему браузеру).
- [x] 4.5 **B** Учитель: CRUD учеников `/api/teacher/students`, выдача/перевыпуск
      приглашения, деактивация (отзыв всех refresh-токенов).
- [x] 4.6 **B** Приглашение: `GET /api/auth/invites/{token}`, `POST .../accept`
      (ученик задаёт логин+пароль).
- [x] 4.7 **B** `identity.api`: `UserDirectory` (teacherId, findStudent(s), currentStudents),
      события `StudentRegistered`, `StudentActivated`,
      `StudentDeactivated`.
- [x] 4.8 **F** `core/auth`: `AuthService` (signals), `authInterceptor` (Bearer + авто-refresh
      при 401), `roleGuard`, восстановление сессии при загрузке приложения.
- [x] 4.9 **F** Страницы: вход, принятие приглашения, смена пароля; учитель — реестр учеников
      (таблица, создание, приглашение со ссылкой и копированием, деактивация).

## Этап 5. Подсистема учёта оплаты (`billing`)

- [x] 5.1 **B** Схема `billing`: `student_accounts` (тариф за занятие), `lessons`
      (дата, длительность, цена, статус: CONDUCTED/MISSED/CANCELLED, тема),
      `payments` (сумма, дата, способ, комментарий). Валюта и часовой пояс инстанса — настройки.
- [x] 5.2 **B** Правила: баланс = оплаты − начисления; пропуск без предупреждения оплачивается;
      ошибочные записи не удаляются, а отменяются/аннулируются и остаются в истории.
- [x] 5.3 **B** Учитель: `/api/teacher/billing/**` — тарифы, журнал занятий, оплаты,
      баланс по ученикам, должники, отчёт за месяц.
- [x] 5.4 **B** Ученик: `/api/me/billing` — свой баланс, занятия, оплаты.
- [x] 5.5 **B** События `LessonRecorded`, `LessonCancelled`, `PaymentRecorded`, `PaymentVoided`
      (с балансом после операции); слушатель `StudentRegistered` → открыть счёт ученика.
- [x] 5.6 **F** Учитель: сводка по балансам, история ученика с отменой/аннулированием, запись
      занятий и оплат, отчёт за месяц. Ученик: карточка баланса, история.

## Этап 6. Подсистема домашних заданий (`homework`)

- [x] 6.1 **B** Схема `homework`: `assignments` (заголовок, markdown-описание, срок),
      `tasks` (задание × ученик, статус ASSIGNED/SUBMITTED/RETURNED/ACCEPTED, оценка,
      комментарий), `submissions` (версии ответов), `attachments` (метаданные файлов).
- [x] 6.2 **B** Жизненный цикл: выдать (одному/нескольким ученикам) → сдать → вернуть на
      доработку / принять с оценкой. Запрет недопустимых переходов в домене.
- [x] 6.3 **B** Вложения: загрузка (лимит размера, whitelist типов), хранение через
      `FileStorage` (`/data/files/homework`), скачивание с проверкой прав.
- [x] 6.4 **B** Учитель: `/api/teacher/homework/**`; ученик: `/api/me/homework/**`.
- [x] 6.5 **B** События `HomeworkAssigned`, `HomeworkSubmitted`, `HomeworkReviewed`,
      планировщик `HomeworkDueSoon` (за N часов до срока).
- [x] 6.6 **F** Учитель: список заданий, редактор (markdown + превью), выдача ученикам,
      очередь на проверку, проверка. Ученик: мои задания, сдача с файлами, статус и отзыв.

## Этап 7. Подсистема уведомлений (`notifications`)

- [x] 7.1 **B** Схема `notifications`: `inbox` (уведомления в ЛК), `channel_links`
      (привязки получателя к Telegram/VK/MAX, флаг «включено» вместо отдельных `preferences`),
      `link_codes` (одноразовые коды привязки, хранятся хеши), `deliveries` (outbox: статус,
      попытки, следующая попытка, ошибка).
- [x] 7.2 **B** Ядро: `NotificationService` → запись в inbox + постановка доставок
      в outbox; `DeliveryDispatcher` (планировщик, ретраи с экспоненциальной задержкой).
      SPI `MessengerChannel` для внешних каналов.
- [x] 7.3 **B** Слушатели событий `homework`/`billing`/`identity` → тексты уведомлений
      (шаблоны, русский язык).
- [x] 7.4 **B** Telegram-бот: Bot API через `RestClient`, long polling (работает без
      публичного URL), привязка через deep-link `t.me/<bot>?start=<code>`, отвязка `/stop`.
- [x] 7.5 **B** ЛС мессенджеров: адаптеры VK (сообщения сообщества, Bots Long Poll) и MAX
      (бот, deep-link `max.ru/<bot>?start=<code>`), включаются переменными окружения;
      привязка через одноразовый код.
- [x] 7.6 **B** REST: `/api/me/notifications` (список, прочитать, прочитать все, счётчик),
      `/api/me/channels` (код привязки, пауза, отвязка), `/api/teacher/notifications/broadcast`.
- [x] 7.7 **F** Колокольчик с счётчиком (опрос), страница уведомлений, настройки каналов
      (кнопка «Подключить», коды для VK/MAX), рассылка от учителя.

## Этап 8. Подсистема интеграции с ИИ (`ai`)

- [x] 8.1 **B** SPI `LlmClient` + адаптеры: Anthropic Messages API (официальный Java SDK,
      по умолчанию `claude-opus-5`, structured outputs, серверные fallbacks) и OpenAI-совместимый
      (OpenAI, OpenRouter, Ollama, LM Studio и др.); выбор провайдера — переменными окружения;
      модуль выключен, если провайдер не настроен.
- [x] 8.2 **B** Сценарии: генерация черновика ДЗ (тема, уровень, число задач) и черновик
      проверки ответа (комментарий + предлагаемая оценка + принять/вернуть). Промпты — ресурсы
      модуля с версией в имени.
- [x] 8.3 **B** Учёт использования: схема `ai` (`requests`: фича, модель, токены, статус),
      месячный лимит токенов, `GET /api/teacher/ai/status|usage`.
- [x] 8.4 **F** Кнопки «Сгенерировать с ИИ» в редакторе ДЗ и «Черновик проверки» на странице
      проверки (frontend сам передаёт тексты — `homework` и `ai` не связаны на бекенде),
      страница «ИИ-помощник» с использованием.

## Этап 9. Эксплуатация и надёжность

- [x] 9.1 **B** Бэкапы: ежедневный SQL-дамп БД (H2 `SCRIPT`, переносим между версиями H2) +
      архив файлов, ротация, ручной запуск и скачивание учителем, восстановление при старте из
      `<data>/restore/` с откатом при ошибке, инструкция — `docs/operations.md`.
- [x] 9.2 **B** Безопасность: security headers/CSP (Spring и nginx), rate limiting
      `POST /api/auth/**`, аудит входов (логгер `teacherbox.audit`), проверка размеров запросов
      (лимиты multipart, `@Size` на всех DTO). Ночная уборка истёкших токенов и приглашений.
- [x] 9.3 **F** Страница «Настройки» учителя: статус интеграций (Telegram/VK/MAX/ИИ),
      неудачные доставки уведомлений (кому, куда, ошибка), бэкапы, профиль.
- [x] 9.4 **D** Логи в stdout (JSON — `LOGGING_STRUCTURED_FORMAT_CONSOLE=ecs`), ротация логов
      Docker, graceful shutdown, лимиты памяти/CPU в compose.

## Этап 10. E2E, CI и релиз

- [x] 10.1 E2E (Playwright) на `compose.single.yaml` (`scripts/e2e.sh`, каталог `e2e/`): вход
      учителя, создание ученика, принятие приглашения, выдача и сдача ДЗ, запись оплаты,
      уведомление в ЛК.
- [x] 10.2 CI (GitHub Actions, `.github/workflows/ci.yml`): verify backend/frontend, сборка
      обоих вариантов образов и проверка nginx, e2e.
- [x] 10.3 README: установка на сервер/NAS за 5 минут, HTTPS, обновление, бэкап/восстановление,
      настройка Telegram/VK/MAX и ИИ.
- [x] 10.4 Версионирование (SemVer, `CHANGELOG.md`), версия 1.0.0, тег `v1.0.0`.

---

# Версия 1.1

Цели: расписание с напоминаниями и календарями, работа Telegram из РФ через прокси,
содержательные главные страницы, простой путь учителя в уведомлениях, Gemini, роль
администратора для отладки. Этапы идут в порядке зависимостей: прокси нужен Telegram, ИИ и
Google; главная страница собирается из виджетов расписания, оплат, заданий и уведомлений.

Новые решения: ADR-0008 (расписание и календари), ADR-0009 (настройки интеграций в БД и
исходящий прокси), ADR-0010 (администратор и диагностика); дополнения в ADR-0006 (Gemini)
и ADR-0007 (FullCalendar).

## Этап 11. Исходящий прокси для внешних интеграций

- [x] 11.1 **B** `shared.http.OutboundProxy`: разбор `http://[user:pass@]host:port` и
      `socks5://host:port`, фабрика HTTP-клиента для `RestClient` (JDK `HttpClient` не умеет
      SOCKS — с прокси используется `HttpURLConnection` + `java.net.Proxy`, HTTP/1.1),
      пароль прокси не попадает в логи. Тесты с локальными HTTP- и SOCKS5-прокси.
- [x] 11.2 **B** Telegram через `TEACHERBOX_NOTIFICATIONS_TELEGRAM_PROXY`: long polling и
      отправка; проверка связи (`getMe`) при старте — понятная ошибка в логе и статус в
      «Настройках». VK и MAX всегда ходят напрямую (российские сервисы).
- [x] 11.3 **B** ИИ через `TEACHERBOX_AI_PROXY`: Anthropic SDK (`proxy` + `proxyAuthenticator`)
      и OpenAI-совместимые провайдеры — API Anthropic, OpenAI и Gemini из РФ недоступны.
- [x] 11.4 **D** `extra_hosts: host.docker.internal:host-gateway` в compose; документация:
      прокси на хосте, прокси в соседнем контейнере (xray/sing-box с входом SOCKS5/HTTP),
      альтернатива — свой адрес Bot API (`..._TELEGRAM_API_URL`, реверс-прокси на зарубежном
      VPS). `.env.example`, README, `docs/operations.md`.

## Этап 12. ИИ: Gemini

Итог исследования: **подписку Google AI Pro подключить нельзя.** Её преимущества действуют
только в веб-интерфейсе Google AI Studio; вызовы Gemini API из приложений оплачиваются
отдельно; использование OAuth-токенов Gemini CLI сторонними программами нарушает условия
Google (с февраля 2026 — блокировка аккаунтов, в том числе платных). Официального способа
для сторонних приложений нет. Законный путь — ключ Gemini API из Google AI Studio
(бесплатный уровень с лимитами или оплата по токенам).

- [x] 12.1 **B** Провайдер `gemini`: пресет OpenAI-совместимого адаптера
      (`https://generativelanguage.googleapis.com/v1beta/openai/`, модель по умолчанию —
      `gemini-3.8-flash`), проверка structured outputs на схемах черновиков и причин отказа
      (safety → `ai.refused`); если совместимый endpoint не справляется со схемами — отдельный
      адаптер на официальном Java SDK `google-genai`. Тесты на локальном HTTP-сервере.
- [x] 12.2 **F/D** Провайдер и модель на странице «ИИ»; README: получение ключа, недоступность
      в РФ (→ `TEACHERBOX_AI_PROXY`), лимиты бесплатного уровня и то, что на бесплатном уровне
      Google может использовать тексты запросов (ответы учеников) для улучшения своих продуктов.

## Этап 13. Модуль расписания (`schedule`) — backend

- [x] 13.1 **B** ADR-0008, модуль `schedule` (зависит от `shared`, `identity::api`), схема
      `schedule`: `series`, `lessons`, `change_requests`, `feeds`; таблица зависимостей в
      AGENTS.md (`billing` и `notifications` получают `schedule::api` — только события).
- [x] 13.2 **B** Домен: занятие (ученик, начало, длительность, тема, ссылка на онлайн-урок;
      SCHEDULED → CONDUCTED / MISSED / CANCELLED), еженедельная серия (дни недели и местное
      время в поясе инстанса — переход на летнее время не сдвигает уроки), занятия серии
      создаются на 12 недель вперёд и продлеваются ночью, изменение серии «с этой даты»,
      предупреждение о пересечении занятий.
- [x] 13.3 **B** Перенос и отмена: учитель — сразу; ученик — запрос (перенос на предложенное
      время или отмена, комментарий), учитель подтверждает или отклоняет. Поздняя отмена
      (позже `TEACHERBOX_SCHEDULE_LATE_CANCELLATION` до начала) — учитель решает, считать ли
      её пропуском.
- [x] 13.4 **B** Итог занятия «Проведено» / «Пропуск» → событие → `billing` записывает занятие
      в журнал по тарифу ученика (идемпотентно по id занятия расписания); исправление итога
      отменяет запись журнала.
- [x] 13.5 **B** События `LessonScheduled`, `LessonRescheduled`, `ScheduledLessonCancelled`,
      `LessonCompleted`, `ChangeRequested`, `ChangeRequestResolved`; напоминания
      `LessonStartingSoon` за интервалы `TEACHERBOX_SCHEDULE_REMINDERS` (по умолчанию
      `PT24H,PT1H`), по одному на интервал.
- [x] 13.6 **B** REST: `/api/teacher/schedule/**` (занятия за период, занятия и серии,
      итоги, запросы), `/api/me/schedule/**` (свои занятия, запросы); тесты «чужой ученик →
      403/404».
- [x] 13.7 **B** Подписка на календарь (ICS, RFC 5545) по секретной ссылке
      `/api/public/schedule/{token}.ics`: лента учителя и лента каждого ученика, перевыпуск
      ссылки. Работает в Google, Apple и Яндекс Календаре без настройки интеграций.
- [x] 13.8 **B** `notifications`: слушатели событий расписания — назначение, перенос, отмена,
      запросы, напоминания ученику и учителю (время в поясе инстанса, ссылка на урок).

## Этап 14. Расписание — frontend

- [x] 14.1 **F** FullCalendar 7 (MIT, Angular 16–22, только бесплатные виды), дополнение
      ADR-0007; русская локаль, оформление под Aura, работа при текущем CSP.
- [x] 14.2 **F** Учитель: раздел «Расписание» — неделя / месяц / список, создание занятия и
      серии, перенос перетаскиванием и в диалоге, отмена, отметка итога, запросы учеников,
      ссылка на ICS.
- [x] 14.3 **F** Ученик: ближайшие занятия и календарь, запрос переноса или отмены, ссылка
      подписки на календарь.
- [x] 14.4 E2E: занятие → запрос переноса → подтверждение → «Проведено» → баланс ученика.

## Этап 15. Google Календарь учителя

- [x] 15.1 **B** Подключение через OAuth 2.0 (кнопка «Подключить Google»): OAuth-клиент типа
      «веб-приложение» в Google Cloud учителя, client id/secret задаются в интерфейсе или в
      переменных окружения; redirect `<адрес портала>/api/public/schedule/google/callback`
      (адрес, по которому открыт портал; нужен HTTPS, без него — однократное подключение через
      `http://localhost` по SSH-туннелю), `state` против CSRF, refresh-токен хранится в БД, отзыв при отключении.
      Минимальный scope `calendar.app.created`: Teacher Box создаёт свой календарь
      «Teacher Box» и управляет событиями только в нём.
- [x] 15.2 **B** Односторонняя синхронизация Teacher Box → Google: outbox (создание / изменение
      / удаление событий, повторы, 404/410, истёкший или отозванный токен → статус «нужно
      переподключить» и уведомление учителю), `RestClient` + `MockRestServiceServer`,
      прокси `TEACHERBOX_SCHEDULE_GOOGLE_PROXY`.
- [x] 15.3 **B** Проверка занятости (по желанию, дополнительный scope `calendar.freebusy`):
      при планировании — предупреждение о пересечении с личными событиями учителя.
- [x] 15.4 **F** «Настройки» → «Google Календарь»: пошаговая инструкция с адресом для
      redirect, ввод client id/secret, «Подключить» / «Отключить», статус и ошибки
      синхронизации.
- [x] 15.5 **D** README: проект в Google Cloud, Calendar API, экран согласия (перевод в
      Production — иначе Google отзывает доступ через 7 дней; предупреждение «приложение не
      проверено» для личного использования допустимо), OAuth-клиент.

## Этап 16. Уведомления: простой путь учителя

Что мешает сейчас: боты настраиваются только переменными окружения и перезапуском; раздела
уведомлений нет в меню учителя; не видно, кто из учеников подключил мессенджер; нельзя
выбрать, о чём уведомлять; неудачные доставки показаны на другой странице.

- [x] 16.1 **B** ADR-0009. Настройки каналов в БД: токены ботов задаются в интерфейсе
      (переменные окружения по-прежнему работают и имеют приоритет), проверка токена
      (`getMe` / `groups.getById` / `/me`), перезапуск адаптера и long polling без перезапуска
      приложения. Токены не отдаются в браузер.
- [x] 16.2 **B** Предпочтения получателя: какие типы уведомлений дублировать в мессенджеры
      (в личном кабинете — всегда), «тихие часы» (доставка откладывается до утра). Для
      учителя — статус подключения мессенджеров по ученикам и история рассылок.
- [x] 16.3 **F** Пункт «Уведомления» в меню учителя, вкладки: «Входящие», «Сообщения
      ученикам» (рассылка и история), «Мессенджеры» (мастер: создать бота по инструкции →
      вставить токен и проверить → подключить свой аккаунт кнопкой → тестовое сообщение),
      «Ученики» (кто что подключил, проблемы доставки, «Напомнить подключить»), «Что
      присылать».
- [x] 16.4 **F** Ученик: предложение подключить мессенджер после принятия приглашения и на
      главной; выбор, что присылать.

## Этап 17. Главная страница

- [x] 17.1 **B** Сводки модулей (каждый — только о своих данных): `billing` — долг, должники,
      поступления за месяц; `homework` — на проверке, просрочено, скоро срок; `schedule` —
      занятия сегодня и на неделе, неотмеченные, запросы; для ученика — свои сводки.
- [x] 17.2 **F** Учитель: «Сегодня» (занятия с быстрыми действиями), «Требует внимания»
      (неотмеченные занятия, работы на проверку, запросы переноса, сбои доставки), «Финансы»,
      последние уведомления, чек-лист первого запуска (ученик → тариф → мессенджер → первое
      занятие). Виджеты живут в своих фичах, главная только собирает их.
- [x] 17.3 **F** Ученик: ближайшее занятие (ссылка на урок, «Перенести»), задания со сроками,
      баланс, последние уведомления, подключение мессенджера.

## Этап 18. Администратор и диагностика

- [x] 18.1 **B** ADR-0010. Роль `ADMIN`: одна учётная запись из
      `TEACHERBOX_IDENTITY_ADMIN_LOGIN/PASSWORD` (пароль не задан — администратора нет);
      доступны только `/api/admin/**` и смена своего пароля. Администратор не видит данные
      учеников (имена, оплаты, работы) — только технические данные и идентификаторы. Его
      действия пишутся в аудит.
- [x] 18.2 **B** Журнал: файлы в `/data/logs` (JSON, ротация по размеру), идентификатор
      запроса (`X-Request-Id`, MDC, поле в ProblemDetail — «код ошибки», который учитель
      может назвать администратору), ошибки браузера отправляются на сервер
      (`/api/client-errors`, ограничение частоты).
- [x] 18.3 **B** `/api/admin/**`: поиск по журналу (период, уровень, логгер, текст, код
      запроса); смена уровня логирования на лету с автоматическим возвратом; состояние
      (версия, время работы, память, диск, размер БД, health); незавершённые события
      Modulith (просмотр, повторная отправка); неудачные доставки (повтор); журнал запросов к
      ИИ (без текстов); проверка связи с интеграциями (Telegram через прокси, ИИ, Google);
      диагностический архив для разработчика (журнал, настройки без секретов, версии).
- [x] 18.4 **F** Область `/admin/**`: «Журнал», «Состояние», «События», «Интеграции»,
      «Диагностика». Код ошибки в сообщениях об ошибках у учителя и ученика.
- [x] 18.5 **D** `docs/operations.md`: включение администратора, работа с журналом, что
      отправлять разработчику.

## Этап 19. Выпуск 1.1

- [x] 19.1 E2E: расписание, мастер подключения Telegram (эмулятор Bot API), главные
      страницы, вход администратора и журнал.
- [x] 19.2 README, `CHANGELOG.md`, версия 1.1.0, тег `v1.1.0`.

---

# Версия 1.2

Цели: групповые занятия, видеовстречи в Яндекс Телемосте, доски Холста у учеников и групп,
работа через ботов для учеников и учителя, справка по приложению. Этапы идут в порядке
зависимостей: группы нужны расписанию, встречам и доскам; основа диалогов в ботах — до
действий модулей; справка пишется последней, когда интерфейс устоялся.

Итоги исследования:

- **Яндекс Телемост.** API (`https://cloud-api.yandex.net/v1/telemost-api`, OAuth-токен
  Яндекс ID со scope `telemost-api:conferences.*`) доступен только пользователям Яндекс 360
  для бизнеса с аккаунтом на домене организации. С личным аккаунтом ссылка встречи создаётся в
  Телемосте вручную. Приложение для ПК открывается ссылкой
  `telemost://https://telemost.yandex.ru/j/<номер>` — так его открывает сам веб-клиент
  Телемоста. Схема не документирована, поэтому рядом всегда есть обычная ссылка (её страница
  тоже предлагает открыть приложение).
- **Холст.** Публичный API (персональные токены) даёт только чтение журнала аудита
  организаций, API для досок нет. Доски умеет менять только локальный MCP-сервер (бета)
  приложения Холст для ПК (`127.0.0.1:47641`), с сервера портала он недоступен. Поэтому в 1.2 —
  доски по ссылкам и перенос материалов через буфер обмена; автоматизация — когда у Холста
  появится серверный API.

Решения: Телемост — оба режима (API для Яндекс 360 для бизнеса и ссылка, вставленная
вручную); постоянная комната у ученика и у группы (в отдельном занятии ссылку можно
заменить); у группы своя цена занятия; Холст — ссылки и копирование.

Новые решения: ADR-0011 (группы и групповые занятия), ADR-0012 (видеовстречи и доски),
ADR-0013 (диалоги с ботами).

## Этап 20. Группы учеников

- [x] 20.1 **B** ADR-0011. `identity`: группа (название, состав, архив), ученик может быть в
      нескольких группах, деактивированный ученик выбывает из групп. Фасад `StudentGroups` и
      события `GroupCreated`, `GroupChanged` (название, состав), `GroupArchived` в
      `identity::api`; REST `/api/teacher/groups/**`; AGENTS.md — описание модуля.
- [x] 20.2 **B** `billing`: цена занятия группы (своя таблица по `groupId`), задаётся при
      создании группы и меняется, как цена ученика.
- [x] 20.3 **F** «Ученики» → вкладка «Группы»: создание, состав, цена; в карточке ученика —
      его группы. Выдача задания и сообщение ученикам — выбор группы подставляет её состав.

## Этап 21. Групповые занятия

- [ ] 21.1 **B** `schedule`: занятие и серия — для ученика или для группы; участники занятия
      хранятся отдельно (`lesson_participants`), у индивидуального занятия один участник
      (миграция существующих занятий). Состав будущих занятий группы следует за составом группы
      (`GroupChanged`), прошедшие не меняются. Пересечения проверяются у учителя и у каждого
      участника.
- [ ] 21.2 **B** Итог группового занятия — посещаемость каждого участника: «был», «пропуск»
      (оплачивается, как у индивидуальных), «предупредил» (не оплачивается). События
      `LessonCompleted` / `LessonCompletionRevoked` — по участнику, события расписания несут
      состав и `groupId`; `billing` списывает цену группы.
- [ ] 21.3 **B** Запросы ученика в групповом занятии: «Не приду» (отмена своего участия;
      поздняя — учитель решает, считать ли пропуском) и предложение переноса (учитель переносит
      занятие для всех, уведомляются все участники). Календари (ICS, Google), напоминания и
      сводки главной учитывают группы.
- [ ] 21.4 **F** Расписание: ученик или группа при создании занятия и серии, состав в
      карточке занятия, отметка посещаемости, групповые занятия отличаются цветом; у ученика —
      пометка «группа» и «Не приду».

## Этап 22. Видеовстречи: Яндекс Телемост (модуль `meetings`)

- [ ] 22.1 **B** ADR-0012. Модуль `meetings` (зависит от `shared`, `identity::api`), схема
      `meetings`: постоянная комната ученика и группы (ссылка, id конференции, как создана).
      `schedule` получает `meetings::api`: ссылка занятия — своя ссылка занятия или комната
      ученика или группы (карточки, напоминания, ICS, Google Календарь). Таблица зависимостей в
      AGENTS.md.
- [ ] 22.2 **B** Подключение Яндекса: OAuth 2.0 Яндекс ID (кнопка «Подключить Яндекс»;
      client id/secret в интерфейсе или `TEACHERBOX_MEETINGS_YANDEX_CLIENT_ID/SECRET`; redirect
      `<адрес портала>/api/public/meetings/yandex/callback`; `state` против CSRF; refresh-токен в
      БД; отзыв при отключении) или готовый токен (`TEACHERBOX_MEETINGS_TELEMOST_TOKEN` — для
      проверки и E2E). Создание конференции `POST /conferences` (зал ожидания для учеников —
      настройка учителя), пересоздание ссылки; 401/403 → «нужно переподключить» и уведомление
      учителю; `MockRestServiceServer`; проверка в разделе администратора (`IntegrationCheck`).
- [ ] 22.3 **B** Режим без API (личный аккаунт): учитель вставляет ссылку Телемоста (или
      другой видеосвязи) в комнату ученика или группы. «Отправить ссылку» — событие
      `MeetingLinkShared` → `notifications` (ЛК и мессенджеры; `notifications` получает
      `meetings::api`, только события).
- [ ] 22.4 **F** «Настройки» → «Видеовстречи»: подключение Яндекса по шагам (приложение в
      Яндекс ID со scope Телемоста, адрес для redirect, client id/secret) или режим без API;
      комната в карточке ученика и группы (создать, заменить, скопировать, отправить).
      Учитель: «Начать урок» в карточке занятия и на главной открывает приложение Телемоста
      (`telemost://…`; на Windows по умолчанию, настройка «Открывать в приложении» хранится на
      устройстве) и доску; рядом — «Открыть в браузере». Ученик: «Войти на урок» на главной и в
      расписании.
- [ ] 22.5 **D** README: Яндекс 360 для бизнеса и API, приложение в Яндекс ID, режим без API
      для личного аккаунта; `.env.example`.

## Этап 23. Доски Холст (модуль `boards`)

- [ ] 23.1 **B** Модуль `boards` (зависит от `shared`, `identity::api`), схема `boards`: доски
      ученика и группы (название, ссылка; ссылки `app.holst.so` распознаются, допустима любая
      https-ссылка). REST учителя и ученика (свои доски и доски своих групп; тест «чужой ученик
      → 403/404»).
- [ ] 23.2 **F** Доски в карточке ученика и группы, «Мои доски» у ученика (главная,
      расписание), доска в карточке занятия и в «Начать урок».
- [ ] 23.3 **F** «На доску» для задания (у учителя) и для результата ИИ (черновик задания и
      черновик проверки): выбор доски ученика или группы → материал копируется в буфер обмена
      (форматированный текст и картинка PNG) → доска открывается в новой вкладке с подсказкой
      «нажмите Ctrl+V». Вставку в Холст проверить вручную (нужен аккаунт Холста).
- [ ] 23.4 **D** README: доска в Холсте, доступ ученику по ссылке, привязка к ученику;
      ограничения — в ADR-0012.

## Этап 24. Диалоги с ботами: основа

- [ ] 24.1 **B** ADR-0013. SPI `shared.chat`: действие бота (id, роль, пункт меню, шаги
      диалога), ввод (текст или нажатая кнопка), ответ (текст и кнопки). Модули реализуют свои
      действия и сами проверяют права, как в REST, и меняют только свои данные; движок в
      `notifications` собирает действия из контекста — как `IntegrationCheck`, без новых
      зависимостей между модулями.
- [ ] 24.2 **B** Кнопки в адаптерах: Telegram — inline-клавиатура, `callback_query` и
      `answerCallbackQuery`, меню команд `setMyCommands`; ВКонтакте — inline-клавиатура с
      `payload` в `message_new`; MAX — `inline_keyboard`, `message_callback` и `/answers`.
      Данные кнопки короткие (лимит Telegram — 64 байта) и ссылаются на состояние на сервере.
      Уведомления тоже могут нести кнопки (колонка в очереди доставки).
- [ ] 24.3 **B** Движок: только привязанные аккаунты (роль — из `identity`), главное меню по
      роли (`/menu`), состояние диалога в БД с истечением, «Отмена» на любом шаге,
      подтверждение перед изменением данных, постраничный выбор ученика и поиск по имени, дата
      кнопками и время текстом («18:30»). Действия учителя — в аудит (`AuditLog`, «через
      бота»); учитель может выключить управление через бота.
- [ ] 24.4 **F** Мастер бота: для ВКонтакте — включить возможности ботов в сообществе;
      вкладка «Мессенджеры» — «Что умеет бот».

## Этап 25. Бот ученика

- [ ] 25.1 **B** `schedule`: «Расписание» (ближайшие занятия со ссылкой на встречу), «Отменить
      занятие» и «Перенести занятие» (занятие → дата и время → комментарий → подтверждение →
      запрос учителю), в групповом занятии — «Не приду»; «Мои запросы» (статус, ответ учителя,
      отозвать).
- [ ] 25.2 **B** `billing`: «Оплаты» — баланс, цена занятия, последние оплаты и списания.
- [ ] 25.3 **B** `meetings` и `boards`: «Войти на урок» (ссылка комнаты) и «Мои доски».

## Этап 26. Бот учителя

- [ ] 26.1 **B** `schedule`: «Сегодня» (занятия дня с кнопками «Проведено» и «Пропуск», у
      группы — посещаемость), «Неотмеченные» (прошедшие занятия без итога).
- [ ] 26.2 **B** `schedule`: «Запросы» — список и кнопки прямо в уведомлении о запросе:
      «Принять», «Отклонить» с ответом текстом; поздняя отмена — «Считать пропуском?».
- [ ] 26.3 **B** `billing`: «Записать оплату» (ученик → сумма: кнопки 1, 4 и 8 занятий по его
      цене или текстом → способ → подтверждение), «Добавить занятие» (проведённое вне
      расписания: ученик или группа → дата → подтверждение → журнал занятий).

## Этап 27. Справка

- [ ] 27.1 **F** Раздел «Справка» (`/teacher/help`, `/cabinet/help`, `/admin/help`): статьи в
      Markdown по ролям, оглавление, поиск; пункт в меню пользователя. Язык простой, без
      технических терминов: короткие шаги «что нажать» и ответы на частые вопросы.
- [ ] 27.2 **F** Статьи учителя: первые шаги, ученики и приглашения, группы, расписание и
      запросы, видеовстречи, доски, задания, ИИ-помощник, оплаты, уведомления и боты, что умеет
      бот, календари (ICS, Google), настройки, частые вопросы. Ученика: вход и пароль,
      расписание и перенос, урок и доска, задания, оплаты, бот. Администратора: журнал и
      диагностика.
- [ ] 27.3 **F** Ссылки на справку в основных разделах: значок «?» в заголовке открывает
      статью в боковой панели, не уводя со страницы; в мастерах — «Подробнее»; бот отвечает на
      `/help` ссылкой на справку. Тест: каждая ссылка ведёт на существующую статью.

## Этап 28. Выпуск 1.2

- [ ] 28.1 E2E: групповое занятие (серия группы → посещаемость → балансы по цене группы),
      комната Телемоста на имитации API и ссылка у ученика, доска ученика, диалог в Telegram
      (ученик кнопками просит перенос → учитель принимает кнопкой в уведомлении), справка из
      раздела.
- [ ] 28.2 README, `CHANGELOG.md`, версия 1.2.0, тег `v1.2.0`.

---

## Будущее

- Двусторонняя синхронизация с Google Календарём.
- Доски Холста без ручного копирования — когда у Холста появится серверный API досок.
- Абонементы/пакеты занятий, онлайн-оплата.
- Вход через Telegram, 2FA для учителя и администратора.
- WhatsApp-канал, e-mail канал.
- ИИ-подсказки для учеников с квотами.
