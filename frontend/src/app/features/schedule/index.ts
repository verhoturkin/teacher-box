/** Public API of the schedule feature (pages are lazy-loaded by the application routes). */
export { MySchedulePage } from './student/my-schedule-page';
export { SchedulePage } from './teacher/schedule-page';
